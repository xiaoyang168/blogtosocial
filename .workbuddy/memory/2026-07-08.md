## 2026-07-08

### 修复 View all 跳转问题
- 问题：首页 Recent Generations 的 "View all" 链接错误指向 `/schedule`
- 修复：链接改为 `/history`，并创建独立的历史记录页面
- 新增：`/history` 页面，支持列表展示、分页加载、删除、恢复功能
- 新增：`/api/history` DELETE 方法，支持用户删除自己的历史记录
- 更新：Navbar 添加 History 导航入口（桌面端+移动端）
- 更新：首页加载时检查 `sessionStorage.restore_post`，自动恢复从 history 页面带回的内容
- 推送：GitHub 已推送，Vercel 自动部署中

### SEO 基础优化
- 更新 `layout.tsx`：完善 metadata（title/description/keywords/authors/publisher），添加 Open Graph 和 Twitter Card，添加 JSON-LD 结构化数据（WebApplication schema）
- 创建 `sitemap.ts`：自动生成 sitemap.xml，包含所有主要页面
- 创建 `robots.ts`：允许首页索引，禁止 sign-in/sign-up 页面索引
- 创建各页面 layout：pricing/terms/privacy/refunds/schedule/history 都有独立的页面级 metadata
- 推送：GitHub 已推送，Vercel 自动部署

### 自动排期定时触发提醒
- 安装 Resend 邮件服务（npm install resend）
- 创建 Vercel Cron 配置（vercel.json）：每 5 分钟检查一次到期任务
- 创建 `/api/cron/schedule-reminders`：查询 15 分钟内到期的 scheduled_posts，发送邮件提醒，标记 reminder_sent=true
- 添加 SQL migration：给 scheduled_posts 表添加 reminder_sent（布尔）和 reminder_sent_at（时间戳）字段，创建索引优化 cron 查询
- 更新 Schedule 页面：在列表中显示提醒状态（Reminder sent / Reminder pending），用铃铛图标和不同颜色区分
- 环境变量：需要 RESEND_API_KEY + CRON_SECRET（Vercel 自动提供 CRON_SECRET）
- 推送：GitHub 已推送，Vercel 自动部署
- 注意：需先在 Supabase SQL Editor 执行 `supabase_migrations/20260708_add_reminder_fields.sql` 添加字段
- 注意：需注册 Resend（resend.com），获取 API key，添加到 Vercel 环境变量 RESEND_API_KEY
